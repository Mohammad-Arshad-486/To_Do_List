# 📝 React TodoList App

A simple React TodoList web app using **React Router v6** for page navigation.  
Includes a clean layout with a **NavBar**, **Home**, and **About** pages.

---

## 🚀 Features

- 🏠 **Home Page** — Displays the main content (e.g., todo list or landing view).  
- ℹ️ **About Page** — Information about the app.  
- 🧭 **NavBar** — Navigation links for switching between pages.  
- ⚙️ **React Router** — Nested routing with `createBrowserRouter` and `Outlet`.  
- 💅 **CSS Styling** — Simple custom styles via `App.css`.